it("should pass", () => {
    expect(true).toEqual(true)
});



// automatically finds files ending in .test.js or .spec.js
// automatically run jest on save with --watch in script in package.json
    // if --watch doesn't work, use --watchAll